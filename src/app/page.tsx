import { NavBar } from '@/components/NavBar';
import React from 'react';

export default function Home() {
    return (
        <React.Fragment>
            <NavBar />
        </React.Fragment>
    );
}
